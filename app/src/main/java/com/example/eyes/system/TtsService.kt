package com.example.eyes.system

import android.content.Context

class TtsService(private val context: Context) : SpeechOutput {
    override fun speak(text: String) {
        // Sẽ hoàn thiện implementation TextToSpeech ở mục 1.4
    }
}
